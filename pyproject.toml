[tool.ruff.lint]
# 在默认规则（E4/E7/E9/F）基础上启用导入排序规则
extend-select = [
    "I",  # isort：导入块排序与格式化
]

[tool.ruff.lint.per-file-ignores]
"routers/**/*.py" = [
    "B008",  # 允许在路由文件中使用 Depends() 作为参数默认值
]
"utils/**/*.py" = [
    "B008",  # 允许在工具文件中使用 Depends() 作为参数默认值
]
